-- Local dev seed only. Run with `supabase db reset` against your local stack.
-- Replace the placeholder user id with a real auth.users.id after signing up
-- once locally (select id from auth.users), then re-run this file manually
-- if you want sample data — it is NOT applied automatically in production.

-- Example (uncomment and fill in a real user id to use):
-- insert into public.calendar_accounts (user_id, provider, display_name, sync_enabled)
-- values ('00000000-0000-0000-0000-000000000000', 'mock', 'Personal (mock)', true);
